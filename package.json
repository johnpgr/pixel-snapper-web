{
  "name": "sprite-fusion-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && cp index.html dist/index.html && cp index.css dist/index.css && cp -r vendor/ dist/vendor && cp -r _locales/ dist/_locales",
    "clean": "rm -rf dist",
    "analyze": "npx @custom-elements-manifest/analyzer analyze --globs components/*.ts --exclude vendor,lib,node_modules --outdir ."
  },
  "customElements": "custom-elements.json"
}
